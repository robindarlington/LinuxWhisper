---
phase: 02-core-dictation-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can press and hold F13 to start recording"
    - "Releasing F13 stops recording and triggers transcription"
    - "Transcribed text appears in the active window within a few seconds"
    - "Works on current session type (X11 or Wayland)"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete dictation pipeline works end-to-end on the user's system.

Purpose: This is the critical validation that Phase 2 delivers its promise. The user physically tests hold-to-talk dictation to confirm hotkey detection, audio capture, transcription, and text injection all work together.
Output: Confirmed working dictation or identified issues for gap closure.
</objective>

<execution_context>
@/home/rob/.claude/get-shit-done/workflows/execute-plan.md
@/home/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-dictation-pipeline/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end dictation pipeline verification</name>
  <files></files>
  <action>
Present the user with verification steps for the complete dictation pipeline.
Ensure the daemon can be started and the user can test hold-to-talk dictation.
  </action>
  <verify>
User confirms dictation works or reports specific issues.
  </verify>
  <done>User has tested the full dictation flow and confirmed it works or identified issues.</done>
  <what-built>Complete dictation pipeline: hold F13, speak, release, text appears in active window. Components: evdev hotkey detection, sounddevice audio capture, faster-whisper transcription (INT8), ydotool/xdotool text injection with auto-detection.</what-built>
  <how-to-verify>
1. Ensure prerequisites are met:
   - User is in `input` group: `groups $USER` should show `input`
   - ydotoold is running (Wayland): `systemctl --user status ydotoold`
   - Or xdotool is installed (X11): `which xdotool`

2. Start the daemon:
   ```bash
   cd /home/rob/Documents/Projects/LinuxWhisper
   .venv/bin/linuxwhisper start
   ```
   Confirm it reports a PID.

3. Check status:
   ```bash
   .venv/bin/linuxwhisper status
   ```
   Should show "running".

4. Open a text editor (or any text input field).

5. Hold F13, speak a short phrase like "hello world", release F13.

6. Observe:
   - Did the text appear in the editor?
   - Was it reasonably accurate?
   - How long did it take after releasing F13?

7. Try a longer phrase: Hold F13, say "The quick brown fox jumps over the lazy dog", release F13.

8. Try a phrase with non-ASCII: Hold F13, say "I went to a cafe", release F13. Note whether "cafe" or "cafe" appears (non-ASCII limitation is documented for Phase 6).

9. Stop the daemon:
   ```bash
   .venv/bin/linuxwhisper stop
   ```
   Confirm clean shutdown.
  </how-to-verify>
  <resume-signal>Type "approved" if dictation works, or describe any issues (e.g., "hotkey not detected", "no text appears", "transcription wrong", "crashes on release")</resume-signal>
</task>

</tasks>

<verification>
1. Daemon starts and reports running status
2. F13 hotkey detected (recording starts on press)
3. Audio captured from microphone
4. Transcription returns reasonable text
5. Text appears in active window
6. Daemon stops cleanly
</verification>

<success_criteria>
- Complete dictation flow works: hold F13 -> speak -> release -> text appears
- Response time is acceptable (text appears within a few seconds of release)
- Daemon starts and stops cleanly
- No crashes or hung states
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dictation-pipeline/02-04-SUMMARY.md`
</output>
